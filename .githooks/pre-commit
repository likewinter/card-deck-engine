#!/usr/bin/env bash
set -euo pipefail

if [ ! -f vendor/bin/mago ]; then
    echo "mago not found — run 'composer install' first" >&2
    exit 1
fi

echo "→ mago lint"
vendor/bin/mago lint

echo "→ mago fmt --check"
vendor/bin/mago fmt --check
